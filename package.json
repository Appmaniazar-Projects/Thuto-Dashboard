{
  "name": "thuto-dashboard",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npx serve -s build -l $PORT"
  }
}